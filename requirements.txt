# Python dependencies for DT Link Toolkit.
#
#   pip install -r requirements.txt
#
# NOTE: SnapPy is intentionally NOT listed here. Full functionality (topological
# simplification, Jones/linking invariants, component-colour matching) needs
# SnapPy running inside Sage, and on the maintainer's macOS setup SnapPy is
# provided by the Sage install, not by pip. Run the tool as:
#
#   sage -python strand_passage_guiV4_0.py ...
#
# Without Sage/SnapPy the app still runs on the continuity engine only and warns
# that Jones polynomials are unavailable. `tkinter` (GUI) ships with CPython.

numpy>=1.21
networkx>=2.6
matplotlib>=3.4

# Needed only for the --nongui spreadsheet output:
pandas>=1.3
openpyxl>=3.0

# Optional for score_diagramV2_0.py: used for optimal point-cloud assignment
# in symmetry scoring. The tool has a greedy fallback when SciPy is absent.
# scipy>=1.7

# Needed only for figure_to_dt.py (extract a DT code from a diagram image):
# scikit-image, SciPy, and Pillow. These typically live in the plain Python 3
# environment rather than Sage, which is why the launcher runs the figure tool
# under Python 3. spherogram (optional) enables its --validate check.
scikit-image>=0.19
scipy>=1.7
Pillow>=8.0

# If you want SnapPy without Sage (limited; Jones needs Sage), you can try:
#   pip install snappy
# but the supported path for this project is `sage -python`.
